# Copy this file to `.env` and edit values for your environment.
# For local development with SQLite the DATABASE_URL below will work.
# To use PostgreSQL, update the URL to point at your Postgres server.

# Example (SQLite - default):
DATABASE_URL=sqlite:///recipes.db

# Example (Postgres):
# DATABASE_URL=postgresql://recipes_user:secret@localhost:5432/recipes_db

# Flask environment settings
FLASK_ENV=development
# When using Flask CLI to run migrations, set FLASK_APP to the module that
# exposes the Flask `app` object. With this project structure set:
# FLASK_APP=recipe_app.manage
FLASK_APP=recipe_app.app
